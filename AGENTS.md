# MM WA Telegram Multiple Accounts — Agent Entry Point

For any implementation, refactor, review, UI, architecture, Electron, IPC, or testing task in this repository:

1. Read `.cursor/skills/mm-wa-telegram/SKILL.md` completely.
2. Follow every applicable file in `.cursor/rules/`.
3. Treat `about.md` as the canonical product specification.
4. Ask the user before changing protected product decisions listed in `.cursor/rules/00-source-of-truth.mdc`.

These instructions apply regardless of the AI client being used.

## User verification handoff

After every code change, modification, or feature addition, end the handoff with a concrete verification section. Include the commands the user should run and the manual behaviors they should verify. If no manual verification is applicable, state that explicitly.
